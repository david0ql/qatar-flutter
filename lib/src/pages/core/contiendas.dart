import 'package:flutter/material.dart';

class ContiendasTab extends StatelessWidget {
  const ContiendasTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
